# Capture Tool frame references

These files are frozen HTML exports of the canonical Web2HTML Library Paper
frames, captured on 2026-08-20. They are visual reference artifacts; the
sibling population templates (`../button-states.html`, `../components.html`,
and `../navigation.html`) preserve the layer names and writable slots required
by the Capture Tool.

Runtime capture never fetches the Paper URL. Refresh these snapshots only when
the canonical reference intentionally changes:

```bash
node "1.2 url-to-paper/scripts/capture-reference-frames.mjs"
```

Source provenance: Paper file `01M04TZDSPQDTXS5YCRKQD66WQ`, page `1-0`.
