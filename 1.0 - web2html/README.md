# web2html

Orchestrator package. Invoke `/web2html` or `Convert <URL> to HTML`. Compat alias: `website-to-html`.

Canonical docs live at the repo root — not here:

- [`pipeline.html`](../pipeline.html) — run order
- [`workflow.html`](../workflow.html) — commands, gates, edge cases
- [`AGENTS.md`](../AGENTS.md) — agent rules
- `SKILL.md` — this package's executable recipe

Install from the repo root: `./scripts/install-skills.sh`
