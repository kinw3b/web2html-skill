# Pillars — versioned rules per stage

Split out of `AGENTS.md` so it loads on demand, not on every turn.
Read the rows for the step you are on before starting it.

**Orchestrator version (web2html):** **2.21.1**

**Sessions.** `1 capture (1.1–1.4)` → `2 build (2.1–2.4)` → `3 QA (3.1–3.4)` → optional `4 pages (4.1–4.4)` → optional `5 astro site (5.1–5.6)`.
2.0 and optional 5.0 are recommended on the strong tier; the tier is advice, never a gate. 1.4, 2.4, and 4.4-opted emit the next session's prompt.
See `model-routing.md`.

| Pillar | Rule |
|---|---|
| **A/4 · Pre-pesticide (2.8.53)** | Live overlay **before** Stage P capture. `pre-pesticide.mjs --url` writes `pre-pesticide.json` (sections / images / forms / landmarks). HUD is `x-paper-prepesticide` so the serializer skips it. Pre = live DOM contract; post = rebuild `qa-overlay` outlines. Homepage only by default. Pitfall #70. |
| **A/4 · Source sections (2.8.161)** | Disk at **1600 / 768 / 390**. After inventory, clip each section into `capture/home-{desktop,768,390}/source-sections/01-slug.png` + `.json` (same `NN-slug` ids at every width). Headless 1.2 collect hides every `x-paper-*` overlay first — Capture Tool must not appear in the clip (Pitfall #104). The pre-pesticide CLI keeps pesticide on. Then **review-sequence**: Paper `01, 02, 03…` is the content order. `01` is the hero. Chrome-only clips are not numbered. `apply-review-sequence.mjs` writes `review-sequence.json`. One `Screenshots` board from the **1600** clips only: **no Headings/title row** — the artboard name already reads Screenshots, so `01` is the first child. Each row is **section-number** + the 1600 shot — clip with `scale: "css"` and cap every row at **1600** so the board matches `home-desktop`; visible Chrome on a HiDPI display otherwise writes 3200px clips (Pitfall #119). Artboard **opacity 50%** so the clips recede next to `home-desktop` (Pitfall #188). **No 768/390 source boards on Paper.** **No Hover States column.** Hover pairs stack on FRAME `Buttons`. Fit-content / stretch. Never `paper-asset://`. Replace an existing blank board. Never a full-page source shot. Never on the lander. Pitfall #71 / #68 / #101 / #119 / #188. |
| **P-0 · Canvas ruler (2.8.121)** | Create one `Ruler · desktop` and nothing else. `Design Library` **must not exist before 1.3**. No `Ruler · 768` / `Ruler · 390` stacks. Frames sit **≥100px** below the bar. Lintel is **20000px**. `draw-rulers.mjs --init`. Pitfalls #37 #42. |
| **1.3 · Single-mine Design Library (2.10.12)** | Runs **immediately after the 1.2 capture**, on the frames that capture wrote — landers at 1600 / 768 / 390 plus FRAME `Navigation` at all three widths. It does **not** wait on the optional 1.4 extension. Inventory every eligible lander and Capture Tool frame once; mine colors plus font families/weights/styles once, and mine **desktop** line-height as `%` (`--line-height-{n}`) plus letter-spacing as `rem` (`--letter-spacing-*`); create exactly one foundations-only `Design Library` first in the row (ahead of `Source`); replace Paper tokens with one deduplicated canonical set; bind exact token matches back across every eligible frame. Bind exact converted line-height (%) and letter-spacing (rem) onto landers too; unmatched stay literals. Opaque brand fills and alpha tints of the same RGB stay separate (`--color-accent` vs `--color-accent-soft`); the wash must not win `--color-accent` by count (Pitfall #154). If Paper already collapsed them onto one var, restore fills from capture HTML via `paper-layer-ids.json` before bind **only when that Paper node already has a fill**. Never invent a fill on a transparent/unpainted frame, never snap a near hue, and never paint a wrapper that already contains two+ named `NN ·` sections (Pitfall #157). Every foundations render also upserts all Paper-supported proposed tokens before painting, so the sheet and Theme panel cannot diverge. Semantic colour bars are 15px high; mined brand swatches remain 104px. Shadows/font styles stay CSS-only because Paper has no matching token type. Near matches remain literal. Pre/post geometry must be identical. Pitfall #108. After bind, stop — do not rebind `apply-theme-tokens.mjs` in a seed-check loop. Color-wash or layout drift is a 1.4 human pin (Pitfall #156). Extra saturation vs a quiet source clip is `color-spill`. Do not dump source PNGs into Paper. 1.4 remains the human walk. Pitfall #156 #157. Twenty seconds of silence on the mine is a kill, not a 3-minute wait (Pitfall #112). **Never run `flatten-paper-buttons.mjs`.** The 1.2 pc-id import tree is the structure. 1.3 is mine + tokens, then pull Buttons/Components. Pitfall #131. |
| **1.3 · Pull Buttons + Components (2.15.0)** | After Design Library + seed QA (`qa/library-seed-qa.json` green), still inside **1.3**. Walk every `NN ·` section on token-seeded `home-desktop`. Write `qa/buttons-components-plan.json` (every row has `sectionId` matching that `NN`), then `pull-desktop-specimens.mjs`. Unique CTAs park on FRAME `Buttons` (legacy name `Hover States`); unique cards/snippets park on FRAME `Components`. Each parked card keeps the review layout: red `NN` badge is the **source section**, not dump order; title is the specimen name. One of a repeating pattern. Empty arrays are allowed when every section was scanned. Then a **light source-CSS hover pass** (`author-button-hover.mjs`) mines `:hover` paint from `source-site/index.html` and parks a Hover cell when that paint exists. Receipts `qa/buttons-components-pull.json` and `qa/button-hover.json` are required to mark 1.3 done. Capture Tool is not this step. Recipe `references/13-buttons-components.md`. Pitfall #205 #207. |
| **A/4 · Semantic import (2.8.44)** | Flatten empty abs strokes/CSS gradients onto parent CSS before `write_html`. A/6 hover HTML uses the same flatten + label collapse (`prepareStateHtml`). Desktop layers named `01 · slug` (no `Desktop section IDs` legend; Paper cannot `create_comment`). Pitfalls #40 #41 #56. |
| **Retired — dump era · Lock QA before retag (2.8.47)** | Not a live 2.0 path. 2.1 authors the page. Do not run `verify-geometry-before-semantic.py` as a ship gate. |
| **B/2b-S · Painted pill is the control (2.8.75)** | After tokens, `bg-accent` / `rounded-*` / `w-max` are chrome. Hoist `a.btn-primary` / `.btn-*` / `data-component=Link` onto that pill. Hover-slot Source · Hover States cards must not ship. Tags mode outlines the whole pill. Pitfall #29 #90. |
| **B/2b-S · Raster nav (2.8.76)** | Never ship `<nav>` whose only content is a full-bleed `<img>` when source has text/link children. Reconstruct logo + links + CTA. `fullpage.png` is visual reference, not the ship layer. Gate `verify-rebuild-trees.py`. Pitfall #91. |
| **B/2b-S · Footer chrome (2.8.77)** | Last chrome band → `<footer>`. Destinations + social icons are `<a href>`. Newsletter is form+email+submit in the painted row. Gate `verify-rebuild-trees.py`. Pitfall #92. |
| **3.2 · GSAP in-view (2.10.11)** | **Mandatory.** `inject-gsap-reveal.py rebuild/index-polish.html` then `verify-gsap-reveal.py .`. Sequential fade+rise as each **parent group**'s top crosses 25% of the viewport from the bottom (`start: "top 75%"`, 80ms stagger of first children + siblings). Heads → text children; grids → articles/divs/lis; mixed inners flatten copy+media. Never skip because 1.4 did not record motion (Pitfall #204). Do not mutate `index.html`. No CSS hide / `class="reveal"` (Pitfall #1). Gate `qa/gsap-reveal-qa.json` `"ok": true`. Recipe `references/gsap-inview.md`. |
| **3.2 · Button hover CSS (2.15.0)** | **Mandatory script.** `apply-hover-css.py .` reads `qa/button-hover.json` (1.3 source CSS) and writes `rebuild/css/hover.css` on existing `library.json` names, linked from `index-polish.html`. Receipt `qa/button-hover-css.json`. Empty `applied` is valid when 1.3 found no paint. Do not skip because Capture Tool did not run. Do not invent UA-blue `a:hover`. 3.1 must not author `hover.css`. Pitfall #207. Recipe `references/hover-22c.md`. |
| **3.2 · Burger open drawer (2.16.0)** | **Mandatory script.** If `index-polish.html` paints a hamburger, `author-nav-drawer.py .` stacks the same desktop links into `#nav-panel`, stamps `[data-nav-toggle]`, and wires `rebuild/css/nav-drawer.css` + `rebuild/js/nav-drawer.js`. Receipt `qa/nav-drawer.json`. Empty `applied` is valid only when no hamburger is painted. Do not skip because Capture Tool did not run. Do not invent a burger Paper never painted. Pitfall #145 #208. Recipe `references/nav-drawer.md`. |
| **3.2 · FAQ accordion (2.17.0)** | **Mandatory script.** If `index-polish.html` paints FAQ rows, `author-faq.py .` wires `[data-action=toggle-faq]`, fills empty answers from `source-site/index.html`, and links `rebuild/css/faq.css` + `rebuild/js/faq.js`. Receipt `qa/faq.json`. Empty `applied` is valid only when no FAQ is painted. Do not skip because Paper signed empty bodies or Capture Tool did not run. Do not invent a FAQ section. Pitfall #79 #209. Recipe `references/faq.md`. |
| **3.2 · Nav dropdown (2.17.0)** | **Mandatory script.** If polish paints a dropdown trigger or a nav label matches a scrape submenu, `author-nav-dropdown.py .` stamps `[data-nav-dropdown-trigger]` and fills the panel. Receipt `qa/nav-dropdown.json`. Empty `applied` is valid only when neither exists. Do not skip for Capture Tool / `--allow-dropdown`. Do not invent a nav label. Pitfall #210. Recipe `references/nav-dropdown.md`. |
| **3.2 · Companion receipts (2.20.1)** | **Mandatory receipts.** `web-design-guidelines` audits `index-polish.html` (never the 2.4 lock) → `qa/web-design-guidelines.md`; `find-animation-opportunities` → `qa/find-animation-opportunities.md`; `apple-design` → `qa/apple-design.md`. Rows are `applied` / `skipped` / `n-a`; a fidelity lock makes a skipped row, not a missing row. `render-polish-report.py` gives each its own card, Emil's C/3.3 card is hover + GSAP (not Semantics), and `verify-polish-passes.py` + `mark --step 3.2 --status done` fail without all three files. Pitfall #215. |
| **3.3 · Semantics (2.10.10)** | Last automated pass before 3.4. Runs on `rebuild/index-polish.html` only. SEO + a11y + lock-safe perf **without retagging signed 2.3 markup**: scrape-only title/description/OG/Twitter/canonical, `alt`, sr-only labels, form names, eager hero / lazy below-fold images, `html lang` from scrape. `semantics_pass.py rebuild/index-polish.html -o rebuild/index-polish.html --freeze-structure` (auto when `qa/fidelity-freeze-24.json` exists). Gate `qa/semantics-pass-qa.json`. No heading/section/footer promote, no type-align mutation, no invented skip-link / marketing meta. Do not write `index.html`. Pitfall #93 #196 #203. |
| **B/2b-S · Type-align QA (2.8.80)** | After heading retag, inherit `text-align` from the node or parent. Census lock align + Tailwind size; Playwright 1600/768/390. This skill does not lock fonts. Skip if Playwright missing. Pitfall #94. |
| **Retired — dump era · Layer names (2.8.103)** | Not a required 2.0 path. 2.1 authors semantic tags from the Paper brief. 2.0 does not join on capture ids. |
| **2.1 · Design System (2.10.7)** | Emit `rebuild/design-system.html` + `rebuild/css/tokens.css` from signed 1.3 `library.json`. Script (`emit-design-system.mjs`), not the model. Chrome uses `var(--color-*)` from tokens, not `#1A1A1A`. `emit_fonts.py` self-hosts Latin woff2 into `css/fonts.css`. Token contract, not the ship. No new `--color` / `--font` names. Folder `2.1 design-tokens` is the token skill, not a second Author. Gate: `design_system_21_gate.py`. |
| **2.2 · Author (2.13.0)** | **First action, unconditional:** `list-paper-comments.mjs` (exit 2 = open threads). Paper `get_jsx` of `home-desktop` → `dump_index_raw.py` → `rebuild/index-raw.html` (real HTML + `tokens.css`, not JSON/JSX `<div />`). Then 4.8 frontend-design authors `rebuild/index-semantic.html` from Paper desktop (plus 768 / 390) using the 2.1 tokens. Aesthetic-risk OFF. No dump metadata on `index-semantic.html`. No skip-link. **No source or external `<a href>`**. Do **not** write `index.html` here. Gate: both files exist, `author_21_gate.py` green. Pitfall #206. |
| **2.3 · Section loop (2.13.0)** | **Never skip.** First action: `seed_index.py` copies `index-semantic.html` → `index.html` (never overwrite; 2.2 done also seeds). Pull the numbered 1.2 clips (`capture/home-{desktop,768,390}/source-sections/NN-slug.png`) and compare each rebuild band to those three files plus `rebuild/index-raw.html`. Run `raw_23_census.py` → `paper_23_disk_gold.py` → `paper_23_rebuild_shots.py --all` → `paper_23_clip_compare.py`. Read `qa/paper-measure/compare/NN-slug-{1600,768,390}-side.png`. Default is serial; spawn at most **two** disk-only workers. **No Paper MCP at 2.3** (Pitfall #202). Controller is the only writer of `rebuild/index.html`. Do not mutate `index-semantic.html`. Pixel-perfect is a **one-pass assist**. Recipe `references/section-23-paper-loop.md`. Gate: `clip-compare.json` + `disk-gold.json` + `rawCompared` + `diskCompared` + `clipCompared` + `section_22_gate.py`. Pitfall #185 #195 #198 #201 #202 #206. **Do not yield to chat mid-2.3.** |
| **2.3 · VALIDATE walk (2.21.0)** | **Mandatory, after APPLY, before the gate.** What the operator used to do by hand at 2.4 from screenshots. Ship order, one band at a time: `paper_23_validate.py . --next` names it, `--id <band> --shoot` re-shoots it at 1600 / 768 / 390 and rebuilds `qa/paper-measure/compare/NN-<band>-{width}-side.png`, **Read all three**, compare with the 1.2 clip + `index-raw.html`, patch that band only, `--record --seen … --verdict … --miss … --patched`, `--shoot` again until every width is `match`; round 3 still off → `--residual`. Receipt `qa/paper-measure/<id>.validate.json`. `section_22_gate.py` fails a band without it, with an empty `seen`, an open last round, a fourth round, or a ship that changed after the last round was shot (fingerprint of `rebuild/index.html` + `rebuild/css`, overlay and 3.x sheets excluded). Controller only. No Paper MCP. No pixel-perfect inside the walk. Pitfall #216. |
| **2.4 · TAGS checkpoint (2.14.0)** | QA overlay pesticide outlines ON by default in TAGS mode. Overlay default is TAGS, not off and not a hidden query. Human receipt. **Marking 2.4 done** snapshots `qa/fidelity-freeze-24.json`, releases the lease, and prints the SESSION 3 prompt (`qa/handoff-3.0.md`). It does **not** create `index-polish.html`. 3.x starts in a new operator-tier session via `resume . --at 3.1 --owner session-3`. Continue label: `Continue to 3.0 polish (3.1–3.4)`. |
| **2.1 · Ship markup integrity (2.10.3)** | `rebuild_write_gate.py` rejects raw `get_jsx-inline-styles` metadata and browser-reparented block content nested in `<p>`. A dump is not a lock assistant. 2.1 may write `design-system.html`; 2.2 writes `index-semantic.html`; 2.3 seeds `index.html`. |
| **B/2b-S · Heading rank (2.8.74)** | Visual titles become real `h1–h6` (type-scale + section role + `text-wrap: pretty`). Gate `qa/heading-pass-qa.json`. Do **not** waive because frames are unnamed. Outlines tags mode (⌥O → tags). Pitfall #89. |
| **3.4 · Compare 2.4 vs polish (2.10.13)** | `open-human-review.py` refreshes C/3.1–3.3 into `rebuild/polish-report.html` and opens **three** Chrome tabs: `rebuild/index.html` (2.4 lock), `rebuild/index-polish.html` (2.4 copy + 3.1–3.3), and the report. Overlay **Polish 3.1–3.3** on the polish file. Last action is a **two-option** CTA: finish the homepage run (`qa/phase-4-skipped.json`, then tidy) or continue to optional Phase 4 (`qa/phase-4-opted.json`, no tidy). Marking 3.4 done without one of those receipts fails. Do not write `NEXT.html`. Never tidy mid-flow (Pitfall #151). Pitfall #63 #203. |
| **4.1 · Sitemap (2.10.13)** | Optional. `scrape-sitemap.py` lists extra same-origin URLs from sitemap.xml / robots.txt. Homepage `/` stays out. No sitemap → crawl homepage hrefs. Receipt `qa/phase-4-sitemap.json`. |
| **4.2 · Capture pages (2.10.13)** | Optional. Same Paper file AND same page as 1.2 — every interior lander parks on the home canvas in one horizontal row under a single `Ruler · pages` below the existing frames. Never `create_page` (no Paper page per URL) / no Navigation / Screenshots / `create_file`. 4.1 already collapsed dynamic detail slugs to one sample per template. Receipt `qa/phase-4-pages.json`. |
| **4.3 · Seed tokens (2.10.13)** | Optional. Serial `apply-theme-tokens.mjs --exact-only --only {slug}-desktop` per page. Never `create_tokens`. Never a second Design Library. Receipt `qa/phase-4-token-seed.json`. |
| **4.4 · Human review (2.10.14)** | Optional. `open-phase-4-review.py` writes `qa/phase-4-review.md`, opens Paper, stop. Two-option: finish (`qa/phase-5-skipped.json`, tidy) or continue to optional Phase 5 (`qa/phase-5-opted.json`, no tidy, then `mark --step 5.1 --status active`). Marking 4.4 done without one of those receipts fails. Opt-in prints `qa/handoff-5.0.md`. |
| **5.1 · Scaffold Astro + shared chrome (2.20.0)** | Optional. Astro-first: `scaffold-astro.py` writes `astro/` from the 3.4 rebuild CSS, fonts, images, client scripts (BaseLayout takes title / description / lang / canonical / ogImage props). `extract-astro-components.py` lifts Header + Footer **once** from `index-polish.html`, then every Paper-backed button / `data-component` / comment-nominated block (Design Library + Capture Tool names, FRAME Components / Buttons / Navigation, `<!-- component: Name -->`, Paper comments naming a reusable component). One-off sections stay inlined. `convert-astro-home.py` writes `src/pages/index.astro` from the polish `<main>` on that chrome. No React. No Tailwind CDN. Receipts `qa/phase-5-scaffold.json` + `qa/phase-5-components.json` + `qa/phase-5-home.json` (all three gate 5.1). Recipe `references/phase-5-astro.md`. |
| **5.2 · Author pages (2.20.0)** | Optional. Serial `dump-interior-raw.mjs` → `rebuild/{slug}-raw.html` (`rebuild_write_gate.py --allow pages` keeps raw dumps only — interiors never land in `rebuild/`). At most two workers each author **only the `<main>` body** of `astro/src/pages/{slug}.astro`, importing BaseLayout + Header + Footer + the 5.1 components they use, from Design System + Paper desktop. No inline `<header>` / `<footer>`. No new `--color` / `--font` names. Hrefs stay `#`. Receipt `qa/phase-5-pages.json` (`record-phase-5-pages.py` rejects missing chrome imports, inline chrome, dump metadata, off-site hrefs). |
| **5.3 · Desktop QA (2.20.0)** | Optional. The ship is the BUILT page. `build-astro-dist.py` (astro build + `file://` relativize → `qa/phase-5-build.json`), then 2.3-style clip loop at 1600 vs `capture/{slug}-desktop/source-sections/`: `paper_23_rebuild_shots.py --page {slug} --ship astro/dist/{slug}/index.html --widths 1600 --all` then `phase_5_compare.py --mode desktop`. Fix in `src/pages/{slug}.astro`, rebuild, reshoot. Serial or max two disk-only workers. **No Paper MCP.** Receipt `qa/phase-5-clip-compare.json`. |
| **5.4 · Responsive (2.20.0)** | Optional. Live 768/390 section clips via `capture-interior-breakpoints.mjs` (never `create_page` / `write_html`). Reshoot `astro/dist/{slug}/index.html` at 768,390, then `phase_5_compare.py --mode responsive`. Real media queries; no `width:100% !important`. Receipt `qa/phase-5-responsive.json`. |
| **5.5 · Wire routes + SEO (2.20.0)** | Optional. `wire-astro-routes.py`: sitemap paths and nav / footer labels → Astro routes across `astro/src` (Header, Footer, pages) — `/` home, `/{slug}/` interiors, leftover `.html` → routes, external / mailto / tel stay `#`. Then per-page scrape-only SEO into that page's frontmatter (title / description / lang / canonical / og:image from the page's own live URL or 4.x copy; homepage takes its own `qa/scrape-meta.json`). Never merge homepage meta onto an interior. No heading / section retag. Then `astro build` + `file://` relativize. Agent never starts `astro dev`. Receipt `qa/phase-5-links.json` + `qa/phase-5-semantics/{slug}.json` + `qa/phase-5-build.log`. |
| **5.6 · Human checkpoint (2.20.0)** | Optional. `open-phase-5-review.py` writes `qa/phase-5-review.md` and opens the built home + first interior from `astro/dist` on `file://`. Human preview server is optional (`npm run preview`). Marking 5.6 done without the review note fails; done tidies and keeps `rebuild/` + `astro/` + `pipeline.html`. |
| **L · Paper font tokens (2.8.72 / 1.2.53)** | Paper `fontFamily` tokens are a native catalog face (`Inter`). Build / `tokens.css` keep the CSS stack (`"Inter", system-ui, sans-serif`) plus installed / `@font-face` files. `get_font_family_info` before `create_tokens`. Unavailable faces skip or fall back — do not write a stack and do not invent a font. Pitfall #88. |
| **L · Tailwind scales (2.10.12)** | Mine colors, families, weights, styles. Desktop also mines line-height as `%` (`--line-height-{n}`) and letter-spacing as `rem` (`--letter-spacing-*`) — unique painted values, not a Tailwind leading/tracking floor. Type **sizes** start as Tailwind defaults; off-scale painted sizes (80px between 72 and 96) inject as `--text-{px}` (`source: mined`) with a red MINED pill. Spacing, radius, elevation stay Tailwind — not a per-node census. Bind exact px to that token; do not snap 80 onto 7xl. Bind exact converted line-height / letter-spacing onto landers. Paper names: `--spacing-0-5` (no dots). Foundations paint `var(--text-6xl)` / `var(--spacing-32)` / `var(--color-ink)` (px is a caption). After brand colors, leftover hexes fill semantic roles, then a Tailwind floor of at least five SEMANTIC colors (`--color-danger` / `--color-warning` / `--color-success` / `--color-info` / `--color-muted`) always ships. Colour grid is four swatches per row. After render-library, `apply-theme-tokens.mjs` binds landers + A/6 + Interactive components (1.2.38). Pitfall #64. |
| **1.3 · Token coverage (2.10.0)** | Straight after the 1.2 capture, `run-design-library-step.mjs` performs the one authoritative mine. Census every node on landers `{page}-(desktop\|768\|390)`, `Buttons` (legacy `Hover States`), `Components`, `Navigation`, legacy A/6, and `Interactive components`; create `Design Library` only after that census. `token-pass-qa.json` and the pre/post `token-geometry-guard.mjs` comparison are required. Bind exact token matches only; unmatched measured values remain listed literals. Tree minus QA file, unlisted raw values, missing targets, or geometry/wrapping drift **fails**. Pitfalls #76 #108. |
| **1.3 · Heading pretty (2.8.51)** | Titles get `text-wrap: pretty` in the 1.3 token-pass (name `heading`/`title`, `--text-xl`+, or ≥28px). Not body/nav/footer/button. Keep it on `get_jsx`; do not strip in the converter. Waiver vs source line breaks. Pitfall #65. |
| **1.3 · Display titles (2.8.52)** | Type sheet always registers `--text-7xl` 72 through `--text-12xl` 224 as **DISPLAY**. Off-scale mined sizes sit in the same large→small list as `--text-80` with a red **MINED** pill — not `--text-display`. Specimens use the primary sans and `Aa`. A 72px hero maps to `--text-7xl`; 80px stays `--text-80`; 160px maps to `--text-10xl`. Pitfall #66. |
| **A/5 · Split headings (2.8.50 / 1.2.31)** | Source line-box titles must be **one Text node** (full copy; `text-align: center` when the live heading is). Walk named `01 ·` sections — do not stop when `childCount` is missing. Scribble `SVG` + `Path` / `SVGVisualElement` is decoration, not an extra. `set_text_content` uses `{ updates: [{ nodeId, textContent }] }`. Decorations are `position: absolute` + `data-decorative` + `pointer-events: none`. `qa-paper` `split-heading` is high and fails A/5. Repair: `merge-split-headings.mjs`. Pitfall #69. |
| **A/5 · Hug section after insert (2.8.51 / 1.2.32)** | After `write_html` into a named section, `update_styles` that section to `height: min-content`. Do not guess a new px height. Artboards may stay `fit-content`. Pitfall #67. |
| **A/5 · Overlay paint order (2.8.71 / 1.2.52)** | Gold is image → dim overlay → type last (JA-0). After flatten+trim, `reorderOverlayPaintOrder` before `write_html`. Do **not** clone the overlay on top. Clone-on-top is for content (CTA forms). Fallback `z-index: -1`. `qa-paper` `overlay-covers-type`. Repair: `fix-overlay-paint-order.mjs` (refuses hotelsix). Separate from overlay-nav (#87), fixed-nav stack (#97), and opacity-0 settle (#85). Pitfall #84. |
| **A/5 · Fixed nav last + origin (2.8.92)** | A live `position: fixed` / sticky overlay nav must be the **last layer** on the lander (later siblings paint on top) and sit at **`x: 0` / `y: 0`**. Nested mid-tree or a non-zero origin hides it behind the hero. Tick Absolute (#87). Not a dim overlay (#84). Pitfall #97. |
| **A/5 · Visual image QA (2.8.51 / 1.2.32)** | Image QA is visual vs live at 1600 / 768 / 390, not only `qa-paper` empty-image. Never `paper-asset://` in `write_html` (use live remote image URLs). Flag `deferred-image`, empty circular `SVG` / `file` overlays, and live rows missing from the HTML capture. Pitfall #68. |
| **A/5 · Icon box metrics (2.8.56 / 1.2.39)** | Match icon size + container width/height + gap from the live/pre-pesticide inventory at that width. Do **not** copy `flex-wrap` / `round(46%)` / 2+1 from 768 onto 1600/1440. `home-desktop` stays 3 columns, no wrap. Tablet/mobile wrap is breakpoint-correct. Pitfall #75. |
| **3.x · Polish file (2.14.0)** | 2.4 `rebuild/index.html` is the lock. `seed_index_polish.py` copies it to `rebuild/index-polish.html` when **3.x goes active**, not at 2.4 done. That copy is unpolished until 3.1–3.3 run. `fidelity_freeze.py` hashes `index.html` and verifies the polish file. Mutating `index.html` in 3.x fails (Pitfall #203). |
| **3.4 · Library classes on the lock (2.10.5)** | **Verify only.** `verify-library-classes.py` before the last human checkpoint. Do **not** run `apply-library-classes.py` — renaming classes after 2.4 breaks the freeze (Pitfall #196). Missing names are a 2.2/2.3 miss: escalate, do not stamp new ones in 3.x. |
| **A/6 · Hover fill is CSS (2.8.90)** | Button hover paint is `background-color` / `border` on the `button` layer. Never a floating absolute Frame+Rectangle over the label. `flattenDecorativeAbs` + `stripAbsolutePaintFill` hoist paint-only wrappers. Pitfall #95. |
| **A/6 · Human hover (2.10.0)** | **1.2** (`hover-reel/scripts/capture-session.mjs`, headless — no window, no HUD) **captures** `home-desktop`, `home-768` and `home-390` through the same path (nothing authored), clips source sections at each width, and parks serializer chrome on FRAME `Navigation` at **all three widths** (`01 · nav-1600` / `02 · nav-768` / `03 · nav-390`), then screenshot-QAs them. Seeds FRAME `Buttons` (legacy `Hover States`) + `Components` + `Navigation` as `width`/`height: fit-content` (never a fixed 1400px board). Capture rows are written on first park. The three review frames sit under `Ruler · desktop` after the captured 768 / 390 frames; runtime never fetches the reference URL. If any destination cannot be created, the extension does not open. **1.4** is the human Paper checkpoint. Capture Tool is leftover live hover (`open-capture`) — 1.3 already filled FRAME `Buttons` and FRAME `Components` from desktop. When Capture Tool runs, `pipeline-progress.py open-capture` opens Google Chrome on the live URL stamped with `paperFileId` + `projectRoot`; a failed open is not a 1.4 done-gate (Pitfall #149). When Capture Tool runs, it is one session: Nav → Hover → Multi → Single → Done. Record then click the navbar and dropdowns; each exact take parks on `Navigation` immediately. Continue stays on Sending until writes finish, then stamps a **compact** overlay last on `home-desktop` / `home-768` / `home-390` at `0,0` (never the live Framer header dump). Page / Desktop / `#main` shells are rejected. Parks **Multi-state Component** and **Single Components** takes on FRAME `Components`, and stacks hover pairs on FRAME `Buttons`. Single Components is `R` → click any exact element, with no tag, role, size, or example-based filtering. List **Navbar** last with a bar icon — not a numbered section. Fixed/sticky → absolute `0,0`, last lander layer, or Skip if it sits in the hero. Never bake the extension chrome into source clips. Capture Tool Done writes `human-hover-done.json` if it ran; 1.4 does not wait on it. **Tags** locks the source tab to 1600 / zoom 100% (same CDP override as 768/390 Navbar). Outlines attach to the live elements so they cannot drift. Scan stamps 1.2 pc-ids and **renames** `home-desktop` from `layer-ids.json` + `paper-layer-ids.json`. It does not wrap new parents and must not overwrite foreign `pc-` wrappers on a second Scan (Pitfall #155). Writes `qa/source-semantics.json` + `.md` + `-map.json`. Pitfall #150. **Auto Mode** keeps painted CTAs and at most one text link per featured box — skip product / blog / collection loops. Hover pairs token-pass against the seeded library. Reopen the side panel when Paper already exists. `--auto` is CI only. Pitfalls #99 #100 #101 #102 #103 #104 #105 #106 #107 #149 #150 #155. |
| **A/6 · Captured outlines** | Already captured live elements keep a red `x-paper-captured-outline` overlay matching the active take card while scrolling, including restored disk takes matched by section and label. It is `x-paper-*` tool chrome with `pointer-events: none`, never a source-element style, so serializers skip it. |
| **A/6 · Mode-scoped outlines** | Captured red outlines render only for takes belonging to the selected mode. Semantics hides all prior-mode outlines and ghosts every visible semantic candidate page-wide in one faded slate color; only the hovered candidate reveals an `h1` / `p` / `img` / `a` / `button` / `form` tag chip. |
| **A/6 · Record blink** | While Record is on, the red header lamp and `x-paper-captured-outline` boxes blink in sync via `hr-record-blink`; `prefers-reduced-motion` keeps them steady. The recording state is mirrored on `html[data-hr-recording="1"]` so overlays outside the HUD can respond. |
| **A/6 · Component parking** | Multi-state and Single Components call the FRAME `Components` writer directly from `persistAndPark`; successful states receive `paperBoard` + `paperParkedAt`. Session startup appends any saved component take missing that receipt, so `saved · Paper missed` cannot silently persist. |
| **A/6 · Unique hover names (2.8.45)** | Height band + paint + hover delta → `btn-primary` / `pill` / `text-link` / `navbar-dropdown`. FAQ off unless `--allow-faq`. Missing hovers are a human call. Compact pills that serialize `flex-start` rewrite to center. Pitfalls #57 #58. |
| **A/6 · Link color hover (2.8.89)** | Footer / nav `text-link` color change **is** a visible Hover cell (`hasVisibleHoverDelta`). Buttons still require paint (`background` / `border` / `box-shadow`). Do not treat a button color tick as hover. |
| **A/6 · Interactive components (2.8.55)** | Navbar hover-to-open dropdowns (`--allow-dropdown`) and 768/390 click burgers own FRAME `Interactive components` — not a section on `A/6 · {page} · states`, not a second board. Desktop: `01 · navbar-dropdown` at **1600**. Burgers: `02 · nav-mobile-768`, `03 · nav-mobile-390`. FAQ stays on A/6. Do not hunt dropdowns; do not invent a desktop burger. Closed \| open, fluid width, flex-start, red badge. **generate → token-pass** (`apply-theme-tokens.mjs`) — specimens use `var(--token)`, not raw px/hex. Ignore this board for lander `rightEdge`. Pitfalls #73 #74. |
| **C/3 · Phone hamburger (2.16.0)** | 3.2 authors the open drawer from the painted burger + desktop links (`author-nav-drawer.py`). Capture Tool `capture-hamburger-states.mjs` is leftover only. Do not skip the sheet for a missing open-nav pair. Pitfalls #43 #74 #208. |
| **C/3 · FAQ + dropdown (2.17.0)** | 3.2 authors FAQ accordion (`author-faq.py`) and nav dropdowns (`author-nav-dropdown.py`) from painted polish + scrape. Capture Tool `--allow-faq` / `--allow-dropdown` is leftover only. Do not skip empty Paper bodies or “do not hunt.” Pitfalls #79 #209 #210. |
| **C/3 · Dropdown width (2.8.26)** | Overlay menus use `left`/`right` gutters and grow with the viewport. Never freeze capture `width` / `max-width` from a 390 box. Pitfall #44. |
| **C/3 · Burger on every tree (2.8.28)** | If an artboard paints a hamburger, wire `[data-nav-toggle]` on that header. Do not bind 390 only. Tablet dumps often keep the icon through 1199. Pitfall #45. |
| **A/6 · Even Slot inset (2.8.20)** | Ancestor-color Slot hugs the control with 24px padding. Never `width: 100%` flush top-left (Pitfall #38). |
| **A/6 · One states board (2.8.21)** | Kinds are sections of `A/6 · {page} · states`. Other review frames park in a horizontal row under the ruler (Pitfall #39). |
| **C/3 · Nav vs footer labels (2.8.18)** | `header a[data-component=NavLink]:hover` only. Footer “Contact” is a FooterLink. Never `@font-face Satoshi-Bold` → the 400 file (Pitfalls #35 #36). |
| **B/4 · In-page outlines (2.8.15 / 2.8.27)** | Every `rebuild/*.html` ships `css/qa-overlay.css` + `js/qa-overlay.js`. Corner **Outlines** / **⌥O** (Option+O; `e.code === "KeyO"`). Desktop: cyan hover chip. **≤767px:** inspector panel with node, id, classes, DOM path; tap to pin. |
| **B/2b · Fills are `<img>` (2.8.31)** | Empty Paper `background-image` boxes download to `rebuild/images/` and become `<img>`. Hoist out of `data-decorative` / `pointer-events: none` wraps. Pitfalls #47 #48. |
| **1.2 postflight · No clipped sections (2.8.167)** | After desktop lands, `run-geometry-postflight.mjs` runs `stretch-root.mjs --prove --artboard home-desktop` only. Agent-authored `home-768` / `home-390` must then pass `breakpoint-shot-qa.mjs` against the disk screenshots. Missing frames or a blank-vs-content miss keeps 1.2 open. Evidence: `qa/stretch-root-evidence.md` + `qa/breakpoint-shot-qa.json`. Do not open optional Capture Tool until this pass is green. Pitfall #55 #168. |
| **1.4 · Human Paper checkpoint (2.21.1)** | **One** yield. `mark --step 1.4 --status active` writes `qa/paper-human-review.md` if missing, **opens Paper**, runs `capture-doctor`, and **opens the browser on the source URL stamped with `paperFileId` + `projectRoot`** so the Capture Tool side panel connects without typing (Pitfall #217). A browser failure is non-fatal: the URL + bridge fixes are printed and 1.4 still goes active. Walk 1600 / 768 / 390, FRAME `Navigation`, FRAME `Buttons`, FRAME `Components`, Design Library. Pin comments. Last action is the 1.4 question modal. Choices: **Done & continue to next step** (mark 1.4 done, continue into 2.1 here) and **Provide hand-off prompt to start fresh session** (`handoff`, do not start 2.1). **Marking 1.4 done requires `qa/paper-human-review.md`**; it releases the controller lease and prints the SESSION 2 prompt (`qa/handoff-2.0.md`). Capture Tool is leftover live hover only — the tab is already open; `pipeline-progress.py open-capture` re-opens it, `capture-doctor` checks the bridge. Not a done-gate. If 1.4 added hover frames, re-run the bind over those frames only — never a second `Design Library`. Problems are **pinned Paper comments on the layer**, not chat prose. A fresh session starts with `pipeline-progress.py resume . --at 2.1 --owner session-2`, never `start` — unless they picked **Done & continue to next step**. |
| **3.0 polish apply (2.10.11)** | Paper remains the brief for structure / IA / homepage-only / `file://`. 2.4 freezes signed 2.3 paint (`qa/fidelity-freeze-24.json`). 3.1–3.2 apply a11y, contrast/readability on existing tokens, anti-slop, hover-if-live, and **mandatory GSAP in-view** on `index-polish.html`. They must **not** diverge from Paper on layout, type size, library class names, icons, or gaps. Do not skip GSAP from a 1.4 archive (Pitfall #204). `frontend-design` at 2.2 and the 2.3 loop keep the fidelity lock. Pitfall #96 #196. |
| **Findings (2.8.84)** | Pitfalls are built-in findings from real runs. Before each step, read that step's accordion on `pipeline.html`. Caught = a gate must stay green. Watch = the agent is the gate. No leftover A/6 hover artboard — pairs on Source · Hover States, dropdowns/burgers on Interactive components. |
| **Live board (2.8.154)** | Invoke `/web2html` or `Convert <URL> to HTML`. Instant first action: `pipeline-progress.py start <template-project>` — the script writes **and opens** that folder's `pipeline.html` **once**. `resume` / `mark` / later sessions never reopen it (Pitfall #191). Do not ship `NEXT.html`. If **start** does not open the board, **stop**. Print the `file://` URI. Never overwrite `web2html/pipeline.html`. `start` quarantines leftover ship HTML (Pitfall #148). Stamp `mark --step 1.2 --status active` before work. Todos use 1.1 … 3.4. Existing progress files with 1.6 are migrated when loaded. Timeline `COMPLETE` keeps a check and full-contrast type; each row uses the `index.html` step icon. |
| **No skipped steps (2.11.0)** | Every child **1.1 → 3.4** runs. **1.4 Paper review**, 2.4 TAGS, polish, and 3.4 are not optional. **Capture Tool** is leftover live hover (`open-capture`), not a 1.4 done-gate. **Phase 4** is optional after 3.4 (`qa/phase-4-opted.json` or `qa/phase-4-skipped.json`). **Phase 5** is optional after 4.4 (`qa/phase-5-opted.json` or `qa/phase-5-skipped.json`). `skip` fails on 1.1–3.4 and is allowed on 4.x / 5.x after that phase's opt-in. If blocked, stay on that step and stop. Pitfall #98. |
| **No pipeline bypass (2.10.0)** | First tool of a new run is `start`; first tool of a continued session is `resume . --at <step> --owner <session>` (`start` force-resets the board). `mark` cannot open 2.1+ while 1.1–1.4 are unfinished. `sync` cannot complete Build from leftover CSS. `rebuild_write_gate.py` is green before any ship HTML. `start` quarantines unauthorized `rebuild/*.html`. A scrape-to-site or Tailwind homepage is a failed run — delete/quarantine it, do not polish it. Pitfall #148. |
| **IDs (2.10.0)** | Design is 1.1 contract, 1.2 capture 1600/768/390 + Navigation at all three + automatic geometry, 1.3 Design Library + tokens + Buttons/Components pull, 1.4 human Paper checkpoint (Capture Tool leftover via `open-capture`) + session 2 handoff. Display `1.4` / `2.1` / `2.2` / `2.3` / `2.4` / `3.1`. Cards are three sibling containers: 1.0 Design, 2.0 Build, 3.0 QA. The 3.x rows never live inside the Build card. The public `index.html` rail shows each step label once, uses one short overview sentence per row, and keeps generous vertical spacing between top-level nodes; procedure stays in `workflow.html`. Public output copy promises plain semantic HTML/CSS/JS plus a reusable CSS token system, never Tailwind output. |
| **One `rebuild/` (2.8.36)** | Geometry, semantic wrap, and hover land on `rebuild/index.html`. Extra `rebuild-semantic/` / `rebuild-hover/` folders fail `verify-rebuild-trees.py`. |
| **3.x · Fixed overlay nav (2.8.131)** | Overlay / live fixed-or-sticky header becomes `position: fixed` on the ship page so it follows scroll. Pitfall #115. Hover if live has it is 3.x / 1.4. |
| **2.1 · Header uses page wrap (2.8.132)** | Logo + nav sit in the same rail class as footer / section max-width. Pitfall #116. |
| **Polish receipts (2.10.11)** | C/3.1–3.3 each write a receipt. `qa/polish-report.html` + `verify-polish-passes.py` + `fidelity_freeze.py verify` + `verify-gsap-reveal.py`. 3.1–3.2 apply a11y / contrast / anti-slop / hover-if-live / mandatory GSAP (`inject-gsap-reveal.py` on `index-polish.html`, `start: "top 75%"`). Paper visual restore (icons, gaps, type) is **2.3 only** — 3.x must not re-run it (Pitfall #196). Do not skip GSAP from a 1.4 archive (Pitfall #204). `no_op_fidelity` is valid when the freeze is clean and GSAP is already injected. |
| **B/3 retired (2.8.38)** | Not a stage. Media → Pass 1 / B/2b-R. JS → Pass 3. B/4 and C/1–2 are gates, not rebuilds. |
| A/6 · Steps 4.0-4.4 | After Stage P has pulled **every contract page and section** into Paper, capture component states page-by-page (**nav links, buttons/CTAs, forms, footer links**) before the Paper build begins. **No GIFs** — default/hover state pairs only. **Visible Chrome** (same launch as Stage P); `--headless` is CI-only. live `whileHover` is not CSS `:hover` (Pitfall #30). **4.1 / 4.4 serialize the links**, not the navbar or footer section (**max 2** pairs from the most-repeated pattern). **4.2 walks Stage P sections one by one** — same CTA label in different sections is a different component. **Do not hunt accordions** (Pitfall #57). New visual patterns get unique tokens (`pill`, `text-link`); repeats share a name. Fixed px `line-height` → **120%** on import; verify alignment via `get_computed_styles`. Buttons/CTAs/forms/nav/footer render as `default | hover` **pairs, 2 per row**. Slots use `height: fit-content` plus a post-write QA refit (Paper clips fixed-px frames). **4.3 is forms only.** FAQ cards (only with `--allow-faq`) hug the A/6 parent — `width: 100%`, never captured px; accordion inners are `flex-start`, not a taller `openH` on collapsed. Navbar dropdowns (`--allow-dropdown`) and 768/390 burgers write closed | open onto FRAME `Interactive components` — not A/6; FAQ stays on A/6; do not hunt dropdowns; generate → token-pass (Pitfalls #73 #74). Homepage-only: `--pages home` **and** `--expected-pages home`. State cells copy the nearest painted ancestor onto a transparent light-text root (Pitfall #27). **P-0 rulers first**; park A/6 80px below `Ruler · desktop`, right of the desktop lander (Pitfall #37) |
| A/7 · 4.x QA | The requested multi-agent QA gate: exactly two read-only reviewer lenses (coverage manifests + Paper geometry) inspect one hashed snapshot of the finished 4.0-4.4 batch and write only `qa/agent-findings/` evidence. One controller accepts current findings, fixes/reimports serially, then both reviewers rerun. Reviewer output is never a canonical receipt. |
| Stage L | Design Library **in Paper** before any build (never skippable) |
| Step 2.2 | **Paper-first:** frontend-design authors `rebuild/index-semantic.html` from Paper + the 2.1 Design System; no freehand / no get_jsx dump / no `index.html` yet |
| **Model routing (2.10.0)** | Three sessions, two handoffs. **2.0 is the only homepage phase recommended on the strong tier** — the one phase that authors something no earlier artifact contains. 1.0 and 3.0 are recommended on the operator tier: every step there lands on a script that opens the artifacts, or stops for a human. Route by whether the gate inspects a real artifact or reads a receipt the model wrote. **The tier is advice, never a gate** — no script checks the model, and a session must never stop to ask for a switch. `references/model-routing.md`. |
| **1.2 · Navigation at three widths (2.10.0)** | FRAME `Navigation` is captured at **1600 / 768 / 390**, not desktop only — `01 · nav-1600` / `02 · nav-768` / `03 · nav-390`, each serialized with the viewport actually at that width so compact and burger paint are real. A missing take at any width keeps 1.2 open. This board is what makes 1.4 optional; every capability added here should shrink what the extension is still needed for. |
| A/4–A/5 · Stage P | Live **pre-pesticide** inventory first (`pre-pesticide.json`). Visible Chrome; homepage at 1600 / 768 / 390 in Paper; `fullpage.png` on disk; **no** Paper sibling screenshot unless asked; anti-freeze settle; **A/5-R stretch-root**. **No clipped sections.** Height Fit is not a waiver. |
| Step 7 | One agent, **two** QA passes vs `source-site/screenshots/` |
| Preview | Chrome on `file://`. **First ship open is the 2.4 TAGS checkpoint** via `open-build-review.py . --stage 2.4`. Overlay default is TAGS. Do not open Chrome on the 2.1 draft. HTTP remains user-opt-in. Pitfall #133. |
| **Retired — dump era · pc-id tree (2.8.143)** | Not a live 2.0 path. 2.0 does not join on capture ids. |
| **Retired — dump era · Inline → CSS (2.8.167)** | Not a live 2.0 path. Paper is the brief, not a JSX dump. |
| **Retired — dump era · 2.2.a–e / Grok Cleanup** | Replaced by 2.1 Design System / 2.2 Author / 2.3 / 2.4. Gated-ladders and Grok Cleanup stay historical plans, not the live recipe. Hover is 3.x / 1.4. The live 2.1 page is a scripted token contract, not the dump-era gallery-as-ship. |
| A/1 · Homepage-only | User says landing page / home only / lander / “not responsive yet” → contract is **1 page (`/`)** plus required viewports. Do **not** force About/Pricing/Contact/404. “Never ask” does not override a later explicit scope change |
| A/4 · 1600 / 768 / 390 | `web-responsive` (pixel-perfect `responsive-scope-playbook.md`; nav/layout change at phone) → default viewports 390×844, 768×1024, 1280×800, 1600 (or 1920). Primary capture stays 1600; **768 and 390 are required Stage P captures**, not Step 3 guesswork |
| **A/5-R** | **Stretch-root QA** after A/5 geometry, before A/6. `stretch-root.mjs --prove` then `qa-paper` with no `frozen-root` findings. Stretching a page artboard must widen section bands; inner site containers keep `max-width`. Evidence: `qa/stretch-root-evidence.md` |
| **Retired — dump era · B/2b-R** | Folded into 2.1 + 2.3. No get_jsx of 768 / 390. 2.3 walks each section vs Paper 1600 / 768 / 390. |
| Stop-at-every-level | When the user asks to stop at every level: after A/1, A/2, A/4, A/8, B/1, B/2a, B/2b, B/4, C/1 write `qa/runs/<run-id>/STOP-<stage>.md` (did / broke / scale patch). Scale learning agent applies the patch |
